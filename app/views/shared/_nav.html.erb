  <nav class="flex sticky bg-white justify-between items-center w-full top-0 py-4 px-8">
    <h1 class="font-bold flex w-1/3 text-2xl uppercase">Li<p class="text-amber-600">Learnv</p>
    </h1>
    <ul class="flex w-[40%] gap-8 font-semibold text-zinc-600">
      <li>
        <a href="#" class="hover:bg-zinc-200 px-4 py-2 rounded-full transition-all">Home</a>
      </li>
      <li>
        <a href="#" class="hover:bg-zinc-200 px-4 py-2 rounded-full transition-all">Courses</a>
      </li>
      <li>
        <a href="#" class="hover:bg-zinc-200 px-4 py-2 rounded-full transition-all">About</a>
      </li>
      <li>
        <a href="#" class="hover:bg-zinc-200 px-4 py-2 rounded-full transition-all">Contact</a>
      </li>
    </ul>
    <div class="flex w-1/4 gap-4">
      <%= render partial: "shared/search_bar" %>
      <%= railsbutton_primary("Daftar", root_path) %>
      <%= button_to "Logout", logout_path, method: :delete, data: { confirm: "Are you sure you want to logout?" }, class: "bg-red-600 text-white font-medium px-4 py-2 hover:opacity-75 transition-all rounded-full w-fit" %>
    </div>

  </nav>